<?xml version="1.0" encoding="utf-8"?>
<androidx.gridlayout.widget.GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:columnCount="2"
    android:padding="5dp"
    android:background="@color/mythemecolor">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardBackgroundColor="#000"
        app:cardCornerRadius="15dp"
        app:cardPreventCornerOverlap="false"
        app:cardUseCompatPadding="false"
        android:id="@+id/parent_layout_In_RecyclerView"
        android:elevation="10dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/imageViewItemInRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="175dp"
                android:layout_gravity="top"
                android:scaleType="centerCrop"
                app:srcCompat="@drawable/e" />

            <TextView
                android:id="@+id/txttitleInRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="2dp"
                android:text="Hier ist the name of the artikel"
                android:textColor="@color/white" />

            <TextView
                android:id="@+id/txtpreisInRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingLeft="8dp"
                android:paddingBottom="2dp"
                android:text="here is the Preis 10€"
                android:textColor="@color/whitebackground"
                android:textSize="10sp" />

        </LinearLayout>
    </androidx.cardview.widget.CardView>






</androidx.gridlayout.widget.GridLayout>